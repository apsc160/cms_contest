#include <stdio.h>

int main(void) {

    int x = 0;
    int y = 0;

    /* TODO: read x and y from standard input */
    scanf("%d %d", &x, &y);

    /* TODO: print x+y to standard output */
    printf("%d\n", x + y);

    return 0;
}